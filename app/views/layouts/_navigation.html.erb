<%= link_to "Tu descuento", root_path, :class => 'brand' %>
<ul class="nav pull-right">
  <% if user_signed_in? %>
	<% if current_user.has_role? :admin %>
          <li id="fat-menu" class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Admin<b class="caret"></b></a>
            <ul class="dropdown-menu">
              <li><%= link_to 'Usuarios', users_path %></li>
            </ul>
          </li>
    <% end %>
    <li><%= link_to "Mis Descuentos", venues_path %></li>
	<li id="fat-menu" class="dropdown">
      <a href="#" class="dropdown-toggle" data-toggle="dropdown">Cuenta<b class="caret"></b></a>
      <ul class="dropdown-menu">
        <li><%= link_to "Perfil", current_user %></li>
        <li><%= link_to "Editar", edit_user_registration_path %></li>
        <li class="divider"></li>
    	<li><%= link_to 'Salir', destroy_user_session_path, :method=>'delete' %></li>
      </ul>
    </li>
  <% else %>
    <li><%= link_to 'Iniciar Sesión', new_user_session_path %></li>
	<li><%= link_to 'Registro', new_user_registration_path %></li>
  <% end %>
</ul>